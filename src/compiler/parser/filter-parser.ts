export function parseFilters(exp: string) {
  let expression;

  let i: number;

  for (i = 0; i < exp.length; i++) {}
  if (expression === undefined) {
    /**遍历结束 i==exp.length */
    expression = exp.slice(0, i);
  } else {
  }

  return expression;
}
